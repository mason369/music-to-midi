# Music to MIDI - Hugging Face Space 依赖
# 注意：不包含 PyQt6（桌面 GUI），使用 Gradio 替代

# PyTorch 核心（版本必须匹配，避免 ABI 不兼容）
torch>=2.3.0,<2.10.0
torchaudio>=2.3.0,<2.10.0
torchvision>=2.3.0,<2.10.0

# NumPy（audio-separator>=0.38.0 要求 numpy>=2，numba 0.64 支持 <2.5）
numpy>=2.0.0
numba>=0.60.0

# YourMT3+ 依赖（使用 lightning 而非 pytorch-lightning，与官方 Space 一致）
lightning>=2.2.1
einops>=0.7.0
transformers==4.45.1
mir-eval>=0.7
deprecated>=1.2.0
smart-open>=6.0.0
nest-asyncio>=1.5.0
scipy>=1.0.0
scikit-learn>=1.0.0
torchmetrics>=0.11.0
wandb

# 模型下载
huggingface_hub>=0.20.0

# MIDI 处理（不需要 python-rtmidi，服务端不播放）
mido>=1.3.0

# 音频分析
librosa>=0.10.0
soundfile>=0.12.0

# 人声分离
audio-separator>=0.38.0
onnxruntime>=1.16.0

# Web UI（锁定版本避免 API schema bug）
gradio==4.44.1

# ZeroGPU 支持
spaces

# 工具
tqdm>=4.66.0
pyyaml>=6.0.0
psutil>=5.9.0
