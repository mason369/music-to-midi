# Intel XPU runtime overlay. Install the common requirements through
# install_xpu.ps1 so CUDA ONNX Runtime entries are filtered deterministically.
# 2.11 is the newest coherent PyTorch XPU trio published with torchaudio;
# its official hardware matrix includes Arc B-Series and Panther Lake.
--extra-index-url https://download.pytorch.org/whl/xpu
numpy==1.26.4
torch==2.11.0+xpu
torchaudio==2.11.0+xpu
torchvision==0.26.0+xpu
onnxruntime-openvino==1.24.1
openvino==2025.4.1
safetensors==0.8.0
