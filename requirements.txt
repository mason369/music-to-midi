# Music to MIDI - Python 依赖
# 唯一转写引擎：YourMT3+ MoE（不再包含 Basic Pitch / Demucs / PANNs / ByteDance 钢琴模型）

# PyTorch 核心（版本锁定以兼容 YourMT3+ 和 pytorch-lightning）
torch>=2.1.0,<2.5.0
torchaudio>=2.1.0,<2.5.0

# NumPy 版本锁定（兼容 numba）
numpy>=1.24.0,<2.0

# YourMT3+ 依赖
pytorch-lightning>=2.2.1
einops>=0.7.0
transformers>=4.30.0
mir-eval>=0.7
deprecated>=1.2.0
smart-open>=6.0.0
nest-asyncio>=1.5.0

# 模型下载
huggingface_hub>=0.20.0

# MIDI 处理
mido>=1.3.0
python-rtmidi>=1.5.0

# 音频分析（节拍/BPM 检测）
librosa>=0.10.0
soundfile>=0.12.0

# GUI
PyQt6>=6.6.0
PyQt6-Qt6>=6.6.0
PyQt6-sip>=13.6.0

# 工具
tqdm>=4.66.0
pyyaml>=6.0.0
