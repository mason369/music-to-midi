# Music to MIDI - Python 依赖
# 默认转写引擎：YourMT3+ MoE；Aria-AMT 为钢琴专用后端，MIROS 为本地可选后端

# PyTorch 核心（三件套版本必须匹配）
torch>=2.4.0
torchaudio>=2.4.0
torchvision>=0.19.0       # onnx2torch 需要 torchvision C++ 算子

# NumPy
numpy>=1.26.0,<2
numba>=0.60.0,<1

# YourMT3+ 依赖
pytorch-lightning>=2.2.1,<3
einops>=0.7.0,<1
transformers==4.48.3
mir-eval>=0.7,<1
deprecated>=1.2.0,<2
smart-open>=6.0.0,<8
nest-asyncio>=1.5.0,<2
wandb>=0.15.0,<1
scipy>=1.10.0,<2
scikit-learn>=1.3.0,<2
torchmetrics>=0.11.0,<2
h5py>=3.10,<4
mirdata>=0.3.8,<1
pretty-midi>=0.2.10,<1
soxr>=0.3.7,<1

# 模型下载
huggingface_hub>=0.20.0,<2
requests>=2.32.5,<3
chardet>=5,<6

# 钢琴专用转写
transkun>=2.0.1,<3
aria-amt @ git+https://github.com/EleutherAI/aria-amt.git
piano-transcription-inference>=0.0.6,<0.1
torchlibrosa>=0.1.0,<0.2

# MIDI 处理
mido>=1.3.0,<2
python-rtmidi>=1.5.0,<2

# 音频分析（节拍/BPM 检测）
librosa>=0.10.0,<1
soundfile>=0.12.0,<1

# GUI
PyQt6>=6.6.0,<7
PyQt6-Qt6>=6.6.0,<7
PyQt6-sip>=13.6.0,<14

# 人声分离 / 六声部分离（BS-RoFormer / BS-RoFormer SW）
# audio-separator 由 install.ps1 / install.sh 单独安装，并锁定到兼容的 no-deps 组合
onnxruntime==1.23.2

# 工具
tqdm>=4.66.0,<5
pyyaml>=6.0.0,<7
psutil>=5.9.0,<8
